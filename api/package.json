{
    "name": "openavis-api",
    "version": "1.0.0",
    "description": "Azure Functions API for OpenAvis",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "watch": "tsc --watch",
        "clean": "rimraf dist",
        "prestart": "npm run build",
        "start": "func start",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@azure/communication-email": "^1.1.0",
        "@azure/cosmos": "^4.9.0",
        "@azure/functions": "^4.0.0",
        "dotenv": "^17.2.3"
    },
    "devDependencies": {
        "@types/node": "^20.x",
        "azure-functions-core-tools": "^4.x",
        "rimraf": "^5.0.0",
        "typescript": "^5.9.2"
    }
}